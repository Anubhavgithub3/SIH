{
  "name": "universal-log-framework",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": ".venv/bin/python -m uvicorn app.main:app --port 8000 || python3 -m uvicorn app.main:app --port 8000"
  }
}
